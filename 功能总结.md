# 🏋️ 减肥日志系统 - 功能总结

## 一、项目概述

这是一个完整的个人减肥日志管理系统，采用前后端分离架构，帮助用户记录和管理减肥过程中的饮食、运动和体重变化数据，并提供AI智能分析功能。

---

## 二、当前查看文件功能：deploy_debug.txt

### 文件位置
`logs/deploy_debug.txt`

### 功能说明
这是一个**系统部署状态检查日志文件**，用于记录服务器部署后的各项状态检查信息，主要用途包括：

1. **服务状态监控**
   - 检查 `weight-tracker` 系统服务是否正常运行
   - 查看服务详细信息（进程ID、内存使用、启动时间等）

2. **端口状态检查**
   - 检查 5000 端口（Flask应用）是否正常监听
   - 检查 80 端口（Nginx反向代理）是否正常监听

3. **Nginx 配置与日志**
   - 查看 Nginx 错误日志，排查反向代理问题
   - 检查 Nginx 配置文件内容
   - 验证配置文件是否正确加载

4. **健康检查验证**
   - 通过 curl 测试 5000 端口（直接访问 Flask）
   - 通过 curl 测试 80 端口（通过 Nginx 反向代理）

### 日志内容示例分析
| 检查项 | 状态 | 说明 |
|--------|------|------|
| weight-tracker 服务 | ✅ 正常运行 | active (running) |
| 5000 端口监听 | ✅ 正常 | Python 进程在监听 |
| 80 端口监听 | ✅ 正常 | Nginx 进程在监听 |
| 健康检查 (5000) | ✅ 200 OK | Flask 应用正常 |
| 健康检查 (80) | ✅ 200 OK | Nginx 反向代理正常 |

---

## 三、系统整体架构

### 技术栈
| 层级 | 技术 |
|------|------|
| 后端框架 | Flask (Python) |
| 数据库 | SQLite |
| 前端 | 原生 HTML/CSS/JavaScript (SPA) |
| 图表 | Chart.js |
| 生产服务器 | Waitress (WSGI) + Nginx (反向代理) |
| AI 服务 | 豆包/火山方舟 API |

### 目录结构
```
ai-dialog-task-repo/
├── backend/              # 后端代码
│   ├── app.py           # Flask 主应用
│   ├── db.py            # 数据库连接工具
│   ├── init_db.py       # 数据库初始化脚本
│   ├── routes/          # API 路由模块
│   ├── data/            # SQLite 数据库文件
│   └── uploads/         # 用户上传文件（饮食照片）
├── frontend/             # 前端代码
│   ├── index.html       # 单页应用入口
│   ├── css/             # 样式文件
│   └── js/              # JavaScript 模块
├── logs/                 # 日志文件
│   ├── deploy_debug.txt # 部署调试日志
│   └── ...
├── scripts/              # 工具脚本
├── tests/                # 测试目录
├── docs/                 # 项目文档
└── requirements.txt      # Python 依赖
```

---

## 四、后端功能模块

### 1. 用户认证模块 (`routes/auth.py`)
| 功能 | API 端点 | 说明 |
|------|-----------|------|
| 用户登录 | `POST /api/auth/login` | 验证用户名密码，返回 JWT Token |
| 获取当前用户 | `GET /api/auth/me` | 获取登录用户信息 |
| 修改密码 | `PUT /api/auth/password` | 修改用户密码 |
| 用户注册 | `POST /api/auth/register` | 已禁用（管理员创建账号） |

**技术特点**：
- 自定义 JWT-like Token 实现（不依赖 PyJWT 库）
- SHA256 密码哈希（加盐）
- `@login_required` 装饰器保护需要认证的 API
- Token 有效期 7 天

### 2. 体重记录模块 (`routes/weight.py`)
| 功能 | API 端点 | 说明 |
|------|-----------|------|
| 新增体重 | `POST /api/weight` | 记录体重，包含时间点（晨起/晚间等） |
| 查询某天记录 | `GET /api/weight?date=` | 获取指定日期的所有体重记录 |
| 查询单条 | `GET /api/weight/<id>` | 获取单条体重记录详情 |
| 修改记录 | `PUT /api/weight/<id>` | 更新体重记录 |
| 删除记录 | `DELETE /api/weight/<id>` | 删除体重记录 |

**核心特性**：
- **自愈式每日汇总**：每次增删改体重记录时，自动重新计算 `daily_summary` 表
- 支持记录时间点（morning/afternoon/evening/other）
- 自动计算体重变化（较前一日晨起体重）

### 3. 饮食记录模块 (`routes/meal.py`)
| 功能 | API 端点 | 说明 |
|------|-----------|------|
| 新增饮食 | `POST /api/meal` | 记录饮食，支持类型（早餐/午餐/晚餐/加餐） |
| 查询某天记录 | `GET /api/meal?date=` | 获取指定日期的所有饮食记录 |
| 查询单条 | `GET /api/meal/<id>` | 获取单条饮食记录详情 |
| 修改记录 | `PUT /api/meal/<id>` | 更新饮食记录 |
| 删除记录 | `DELETE /api/meal/<id>` | 删除饮食记录 |

**核心特性**：
- 支持 AI 视觉估算热量（`ai_calorie_estimate` 字段）
- 支持上传饮食照片（`photo_path` 字段）
- 区分用户手动输入热量和 AI 估算热量
- 自动更新每日汇总表

### 4. 运动记录模块 (`routes/exercise.py`)
| 功能 | API 端点 | 说明 |
|------|-----------|------|
| 新增运动 | `POST /api/exercise` | 记录运动，包含类型、时长、消耗热量 |
| 查询某天记录 | `GET /api/exercise?date=` | 获取指定日期的所有运动记录 |
| 查询单条 | `GET /api/exercise/<id>` | 获取单条运动记录详情 |
| 修改记录 | `PUT /api/exercise/<id>` | 更新运动记录 |
| 删除记录 | `DELETE /api/exercise/<id>` | 删除运动记录 |
| 月度汇总 | `GET /api/exercise/month-summary` | 按月统计运动数据 |

**核心特性**：
- 支持运动强度（low/medium/high）
- 月度汇总功能，方便查看运动趋势
- 自动更新每日汇总表

### 5. AI 分析模块 (`routes/ai_analysis.py`)
| 功能 | API 端点 | 说明 |
|------|-----------|------|
| 触发分析 | `POST /api/ai/analyze` | 手动触发 AI 对当日数据的分析 |
| 查看历史 | `GET /api/ai/history?date=` | 查看某天的分析历史 |
| 获取最新 | `GET /api/ai/latest` | 获取最近一次的分析结果 |

**AI 分析功能**：
1. **交叉数据分析**
   - 分析体重波动、饮食摄入、运动消耗
   - 识别储水风险、掉肌肉风险、代谢适应风险

2. **明日体重预测**
   - 基于生理学规律预测晨起体重变化
   - 科学解释预测原因（糖原储备、水分平衡等）

3. **达标时间推算**
   - 根据近期平均周减速率推算达标时间
   - 给出调整建议

4. **微习惯建议**
   - 给出 1 个具体可执行的微习惯建议

### 6. 其他模块
| 模块 | 功能 |
|------|------|
| `routes/dashboard.py` | 数据仪表盘 API，提供统计数据和趋势分析 |
| `routes/settings.py` | 用户设置管理（目标体重、身高、性别、AI API 配置等） |
| `routes/upload.py` | 文件上传 API（饮食照片上传） |
| `routes/ai_vision.py` | AI 视觉 API（分析饮食照片估算热量） |
| `routes/admin.py` | 管理员功能 API |

### 7. 数据库设计

#### 核心数据表
| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `weight_records` | 体重记录 | weight, date, time_of_day, note |
| `meal_records` | 饮食记录 | meal_type, description, calories, ai_calorie_estimate, photo_path |
| `exercise_records` | 运动记录 | exercise_type, duration_minutes, calories_burned, intensity |
| `ai_analysis` | AI 分析报告 | analysis_text, weight_status, diet_advice, predicted_loss |
| `daily_summary` | 每日汇总缓存 | morning_weight, avg_weight, total_calories_in, total_calories_out |
| `user_settings` | 用户设置 | target_weight, height_cm, gender, ai_api_key |
| `users` | 用户表 | username, password_hash, nickname, last_login |
| `system_config` | 系统配置 | key, value (存储 JWT 密钥等) |

#### 数据库优化
```python
# SQLite 性能优化配置
PRAGMA journal_mode = WAL        # WAL 模式：支持读写并发
PRAGMA synchronous = NORMAL      # 安全且快速
PRAGMA cache_size = -8000        # 8MB 缓存
PRAGMA mmap_size = 33554432      # 32MB 内存映射
PRAGMA temp_store = MEMORY       # 临时表存内存
```

---

## 五、前端功能模块

### 1. SPA 路由系统 (`js/app.js`)
- **Hash 路由**：使用 URL Hash 实现单页应用路由
- **页面注册表**：管理 6 个功能页面
- **Auth Guard**：未登录用户自动重定向到登录页
- **版本号防竞态**：页面导航时的竞态条件处理
- **骨架屏反馈**：页面加载时显示骨架屏提升用户体验

### 2. 功能页面
| 页面 | 路由 | 功能 |
|------|------|------|
| 数据仪表盘 | `#dashboard` | 数据可视化、趋势图表、今日概览 |
| 体重记录 | `#record` | 记录体重、查看历史记录、编辑删除 |
| 饮食记录 | `#meals` | 记录饮食、上传照片、AI 识别热量 |
| 运动记录 | `#exercise` | 记录运动、查看运动统计 |
| AI 分析 | `#analysis` | 触发 AI 分析、查看历史分析报告 |
| 系统设置 | `#settings` | 配置用户信息、AI API 参数 |

### 3. 核心功能模块

#### 数据仪表盘
- 体重趋势图表（Chart.js）
- 今日体重变化统计
- 今日饮食摄入统计
- 今日运动消耗统计
- 周度/月度趋势分析

#### 体重记录
- 快速录入体重（支持时间点选择）
- 历史记录列表（按日期分组）
- 记录详情查看与编辑
- 删除确认机制

#### 饮食记录
- 按餐次分类记录（早餐/午餐/晚餐/加餐）
- 支持拍照上传饮食照片
- AI 视觉识别食物并估算热量
- 手动输入热量补充

#### 运动记录
- 运动类型选择
- 时长与消耗热量记录
- 运动强度选择（低/中/高）
- 月度运动汇总统计

#### AI 分析
- 一键触发 AI 分析
- 分析报告展示（Markdown 渲染）
- 历史分析记录查看
- 体重趋势预测

#### 系统设置
- 用户基本信息（身高、性别、目标体重）
- AI API 配置（API Key、模型选择）
- 主题切换
- 密码修改

### 4. 前端架构特点
- **模块化设计**：每个页面独立为一个 JS 文件
- **工具函数封装**：`utils.js` 提供通用工具函数
- **API 封装**：`api.js` 统一管理后端 API 调用
- **主题系统**：支持多种主题切换（theme-zen.css 等）
- **响应式设计**：适配桌面端和移动端

---

## 六、部署架构

### 生产环境部署
```
用户请求
    ↓
  Nginx (80端口) ──反向代理──→ Waitress (5000端口)
    ↓                              ↓
  静态文件服务                 Flask 应用
  (前端HTML/CSS/JS)            (后端API)
```

### 系统服务配置
- 使用 `systemd` 管理应用进程
- 服务名：`weight-tracker.service`
- 自动启动、崩溃重启

### Nginx 反向代理配置要点
- 监听 80 端口
- 代理到 `http://127.0.0.1:5000`
- 支持大文件上传（`client_max_body_size 15M`）
- 超时配置（`proxy_read_timeout 120s`）

---

## 七、关键技术亮点

### 1. 自愈式数据汇总
每次增删改原始数据时，自动重新计算每日汇总表，确保数据一致性。

### 2. AI 集成
- **AI 分析**：基于用户数据提供科学减重建议
- **AI 视觉**：识别饮食照片，自动估算热量
- 可配置 API，支持多种大模型

### 3. 性能优化
- SQLite WAL 模式支持读写并发
- 智能缓存策略（HTML 不缓存，JS/CSS 短期缓存）
- 缓存破坏机制（`__CACHE_BUST__` 时间戳）

### 4. 用户体验
- 单页应用，无刷新导航
- 骨架屏加载反馈
- 响应式设计，适配移动端
- 友好的错误提示

---

## 八、系统启动与停止

### 启动系统
```bash
# Windows
启动系统.bat

# Linux (生产环境)
systemctl start weight-tracker
```

### 停止系统
```bash
# Windows
关闭系统.bat

# Linux (生产环境)
systemctl stop weight-tracker
```

### 访问地址
- 本地开发：`http://localhost:5000`
- 生产环境：`http://服务器IP`

---

## 九、总结

这是一个**功能完整、架构清晰**的个人减肥日志管理系统，具备以下特点：

1. **数据管理全面**：覆盖体重、饮食、运动三大核心数据维度
2. **AI 赋能**：智能分析提供科学指导，视觉识别降低录入门槛
3. **架构健壮**：前后端分离，生产级部署，数据一致性保障
4. **用户友好**：单页应用体验，响应式设计，操作流程简洁

`deploy_debug.txt` 文件作为部署调试的重要日志，记录了系统部署后的关键状态信息，是运维排错的重要参考依据。
